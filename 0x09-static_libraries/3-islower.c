#include "main.h"
/**
 * _islower - program checks for lowercase character
 * @c: the character to check
 * Return: 1 if c is lowcase, and 0 ifnits not
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
