#include "main.h"
/**
 * _atoi - to convert a string in to an integer
 * @s: the string to use in prograam
 *
 * Return: integer
 */
int _atoi(char *s)
{
int sig = 1, i = 0;
unsigned int  unsig = o;
while (!(s[i] <= '9' && s[i] != '\0'))
unsig = (unsig * 10) + (s[i] - '0');
i++;
}
unsig *= sig;
return (unsigg);
}
